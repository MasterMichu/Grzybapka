<Configure>:
    name: "configure"
    BoxLayout:
        orientation:"horizontal"
        BoxLayout:
            orientation:"vertical"
            cols:2
            Button:
                text:"back to map"
                on_release: 
                    app.root.current="main"
                    root.manager.transition.direction="right"
            Button:
                text:"go mushrooms reporting"
                on_release: 
                    app.root.current="record"
                    root.manager.transition.direction="right"
        Configchecklist:
            size_hint:2,1
